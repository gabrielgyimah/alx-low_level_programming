#include "main.h"

/**
 * mul -  checks for a digit (0 through 9).
 * @a: Holds the value for var a
 * @b: Holds the value for var b
 *
 * Return: Returns result
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
