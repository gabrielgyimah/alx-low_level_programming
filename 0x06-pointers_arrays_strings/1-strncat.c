#include "main.h"
#include <string.h>
/**
 * _strncat - concatenates two strings.
 * @dest: String 1
 * @src: string 2
 * @n: where the concatenation ends for src
 *
 * Return: dest
 */
char *_strncat(char *dest, char *src, int n)
{
	_strncat(dest, src, n);

	return (dest);
}
