#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - outputs the lenght of a string
 *
 * @s: char pointer
 *
 * Return: an int value
 */

int _strlen_recursion(char *s)
{
	int lenght_of_s = strlen(s);

	return (lenght_of_s);
}
